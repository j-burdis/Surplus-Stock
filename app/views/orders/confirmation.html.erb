<div class="p-4 text-center">
  <h1 class="font-bold text-3xl text-darkBlue">Thank you for your order!</h1>
  <p>
    Your order has been successfully placed and will be
    <br> delivered to <%= @order.house_number %>, <%= @order.street_address%>, 
    <%= @order.city %>, <%= @order.display_postcode %>
    <br> on <%= @order.delivery_date.strftime('%A %e %B') %>
  </p>
  <p>Order ID: <%= @order.id %></p>
  <p>Order Total: £<%= (@order.total_amount) %></p>

  <!-- Optionally show more order details -->
  <div class="py-4">
    <%= link_to "Back to Home", root_path, class: "btn-primary" %>
  </div>
</div>
